on: [push, pull_request]

jobs:
  auto-merge:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [14.x]
    steps:
      - uses: mikeal/merge-release@v4.0.7
        env:
          SRC_PACKAGE_DIR: sdk
